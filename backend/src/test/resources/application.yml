spring:
  application:
    name: mulkkam
  profiles:
    include: private
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: never

  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: false

open-weather:
  api:
    key: temp

security:
  jwt:
    secret-key: 090243421414217456af1bab82e65fb2
    expire-length: 36000000
