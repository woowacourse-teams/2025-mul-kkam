<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingHorizontal="4dp"
    android:paddingVertical="6dp">

    <View
        android:id="@+id/view_label"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/selector_mulkkam_chip"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="18dp"
        android:layout_marginVertical="4dp"
        android:textAppearance="@style/title3"
        android:textColor="@color/selector_mulkkam_chip_label"
        app:layout_constraintBottom_toBottomOf="@id/view_label"
        app:layout_constraintEnd_toEndOf="@id/view_label"
        app:layout_constraintStart_toStartOf="@id/view_label"
        app:layout_constraintTop_toTopOf="@id/view_label"
        tools:text="물" />
</androidx.constraintlayout.widget.ConstraintLayout>
